<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title }}</title>
    {% include 'head' %}
</head>
<body>
    {% include 'header' %}

    <main>
        {% yield %}
    </main>

    {% include 'footer' %}
</body>
</html>