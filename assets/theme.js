// Shopify Impact Theme JavaScript

// This is the main JavaScript file for the Shopify Impact Theme.
// Add your theme-specific JavaScript functionality here.

// Example function to initialize theme settings
function initializeTheme() {
    console.log('Impact Theme initialized');
    // Add more initialization code as needed
}

// Document ready function to ensure the DOM is fully loaded
document.addEventListener('DOMContentLoaded', (event) => {
    initializeTheme();
});
